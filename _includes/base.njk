<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ name }}</title>
        <!-- Tailwind CSS -->
        <link rel="stylesheet" href="/css/bundle.css">
    </head>
    <body>
        <div class="p-10">
            <a href="/">
                <h1 class="text-blue-600 text-6xl">{{ info.name }}</h1>
            </a>
            {{ content | safe }}
        </div>
    </body>
</html>